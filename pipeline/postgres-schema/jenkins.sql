CREATE TABLE jenkins_data_b (
	"Time" TIMESTAMP WITHOUT TIME ZONE, 
	build_agent_name VARCHAR NOT NULL, 
	build_branch_name BOOLEAN, 
	build_cause VARCHAR NOT NULL, 
	build_causer VARCHAR NOT NULL, 
	build_exec_time DECIMAL NOT NULL, 
	build_measured_time DECIMAL NOT NULL, 
	build_number DECIMAL NOT NULL, 
	build_result VARCHAR NOT NULL, 
	build_result_1 VARCHAR NOT NULL, 
	build_result_ordinal DECIMAL NOT NULL, 
	build_scheduled_time DECIMAL NOT NULL, 
	build_status_message VARCHAR NOT NULL, 
	build_successful BOOLEAN NOT NULL, 
	build_time DECIMAL NOT NULL, 
	build_user VARCHAR, 
	instance VARCHAR NOT NULL, 
	last_stable_build DECIMAL NOT NULL, 
	last_successful_build DECIMAL NOT NULL, 
	project_build_health DECIMAL NOT NULL, 
	project_name VARCHAR NOT NULL, 
	project_name_1 VARCHAR NOT NULL, 
	project_namespace VARCHAR NOT NULL, 
	project_path VARCHAR NOT NULL, 
	project_path_1 VARCHAR NOT NULL, 
	tests_failed DECIMAL, 
	tests_skipped DECIMAL, 
	tests_total DECIMAL, 
	time_in_queue DECIMAL NOT NULL
);
